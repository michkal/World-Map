import React from "react"
import {WheatherMap} from "../../components/weatherMap"

export const Weather = ({}) => {
    return (
        <div>
            <WheatherMap/>
        </div>
    )
};